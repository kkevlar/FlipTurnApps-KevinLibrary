package com.flipturnapps.kevinLibrary.gui;

import javax.swing.JPanel;

public class KColorChooser extends JPanel{
/*
	private static final long serialVersionUID = 9143730232894971753L;

	private KJCheckBoxPackage boxes;
	private Button b = new Button();
	private boolean n;
	public KColorChooser(Color[] colors)
	{
		super();
		setVisible(false);
		boxes = new KJCheckBoxPackage(colors.length);
		boxes.setBackgrounds(colors);
		boxes.setOneAtATime(true);
		add(boxes);
		add(b);
		b.setText("Continue");
	}
	public Color start()
	{
		setVisible(true);
		while(true)
		{
			n = false;
			while (n == false){};
			n = false;
			int i = ArrayHelper.indexof(true ,boxes.getSelected());
			if (i == -1)
				continue;
			else
			{
				return boxes.getBox(i).getBackground();
			}
		}

	}
	public KJCheckBoxPackage getBoxes(){
		return boxes;
	}
	public JButton getButton()
	{
		return b;
	}
	private class Button extends KJButton
	{

		@Override
		public void performedAction() {
			n = true;

		}

	}

*/
}
